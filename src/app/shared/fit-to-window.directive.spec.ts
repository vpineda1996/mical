import { FitToWindowDirective } from './fit-to-window.directive';

describe('FitToWindowDirective', () => {
  it('should create an instance', () => {
    const directive = new FitToWindowDirective();
    expect(directive).toBeTruthy();
  });
});
